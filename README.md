# Fleet Demo App

This is a demonstration application for use with Rancher Fleet.

## Overview

This repository contains a simple application that can be deployed via Fleet to a Kubernetes cluster. It serves as a basic example of how to structure a repository for GitOps with Fleet.

## Usage

To deploy this application using Fleet, add this repository to a `GitRepo` resource in your Rancher setup.